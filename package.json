{
  "name": "catalyst-magazine-cms",
  "version": "1.0.0",
  "description": "Content Management System for The Catalyst Magazine",
  "main": "server.js",
  "scripts": {
    "build:cloudflare": "node scripts/build-cloudflare.js",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "pages:dev": "npm run build:cloudflare && wrangler pages dev cloudflare-dist",
    "pages:deploy": "npm run build:cloudflare && wrangler pages deploy cloudflare-dist --project-name=catalystmagazine"
  },
  "keywords": ["cms", "magazine", "firebase", "cloudflare"],
  "author": "The Catalyst Magazine",
  "license": "MIT",
  "dependencies": {
    "bundle": "file:./tools/bundle-shim",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "wrangler": "^3.78.0"
  }
}
